type Query {
  girl: Girl
  girls: [Girl]
  stores: [Store]
}
type Mutation {
  addGirl(name: String!, age: Int, storeId: Int): Girl
}
type Store {
  name: String!
  girls: [Girl]
}
type Girl {
  name: String
  age: Int
  storeId: Int
}
